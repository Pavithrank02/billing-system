import { createSlice } from "@reduxjs/toolkit";
const unPaidInvoiceSlice = createSlice({
  name: "unpaid",
  initialState: [],
  reducers: {

  }
})
export const {} =  unPaidInvoiceSlice.actions
export default unPaidInvoiceSlice.reducer
